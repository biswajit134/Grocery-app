name: admin-ci
on:
  push:
    branches: [main]
    paths:
      - 'src/admin/**'
  pull_request:
    branches: [main]
    paths:
      - 'src/admin/**'

  workflow_dispatch:




jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write


    steps:
      - uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3


      - name: Login to Dockerhub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}


      - name: Build admin app and push Docker image
        uses: docker/build-push-action@v7
        with:
            context: ./src/admin
            push: true
            tags: ${{ secrets.DOCKER_USERNAME }}/grocery-admin-app:latest