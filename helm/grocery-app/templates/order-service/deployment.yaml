{{- if .Values.orderService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "grocery-app.componentName" (dict "context" . "component" "order-service") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "grocery-app.componentLabels" (dict "context" . "component" "order-service") | nindent 4 }}
spec:
  {{- if not .Values.orderService.autoscaling.enabled }}
  replicas: {{ .Values.orderService.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "grocery-app.selectorLabels" (dict "context" . "component" "order-service") | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        {{- include "grocery-app.componentLabels" (dict "context" . "component" "order-service") | nindent 8 }}
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      {{- include "grocery-app.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "grocery-app.componentName" (dict "context" . "component" "order-service") }}
      terminationGracePeriodSeconds: 30
      {{- include "grocery-app.backendSecurityContext" . | nindent 6 }}
      containers:
        - name: order-service
          image: "{{ .Values.orderService.image.repository }}:{{ .Values.orderService.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.orderService.port }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.orderService.env.PORT | quote }}
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: {{ include "grocery-app.fullname" . }}-secrets
                  key: mongo-uri-orders
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "grocery-app.fullname" . }}-secrets
                  key: jwt-secret
            - name: PRODUCT_SERVICE_URL
              value: {{ .Values.orderService.env.PRODUCT_SERVICE_URL | quote }}
            - name: NODE_ENV
              value: production
          livenessProbe:
            httpGet:
              path: {{ .Values.orderService.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.orderService.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.orderService.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.orderService.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.orderService.probes.liveness.failureThreshold }}
          readinessProbe:
            httpGet:
              path: {{ .Values.orderService.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.orderService.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.orderService.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.orderService.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.orderService.probes.readiness.failureThreshold }}
          resources:
            {{- toYaml .Values.orderService.resources | nindent 12 }}
          {{- include "grocery-app.containerSecurityContext" . | nindent 10 }}
      {{- if .Values.orderService.podAntiAffinity.enabled }}
      {{- include "grocery-app.podAntiAffinity" (dict "component" "order-service") | nindent 6 }}
      {{- end }}
      {{- with .Values.orderService.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.orderService.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
