{{- if .Values.productService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "grocery-app.componentName" (dict "context" . "component" "product-service") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "grocery-app.componentLabels" (dict "context" . "component" "product-service") | nindent 4 }}
spec:
  {{- if not .Values.productService.autoscaling.enabled }}
  replicas: {{ .Values.productService.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "grocery-app.selectorLabels" (dict "context" . "component" "product-service") | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        {{- include "grocery-app.componentLabels" (dict "context" . "component" "product-service") | nindent 8 }}
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      {{- include "grocery-app.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "grocery-app.componentName" (dict "context" . "component" "product-service") }}
      terminationGracePeriodSeconds: 30
      {{- include "grocery-app.backendSecurityContext" . | nindent 6 }}
      containers:
        - name: product-service
          image: "{{ .Values.productService.image.repository }}:{{ .Values.productService.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.productService.port }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.productService.env.PORT | quote }}
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: {{ include "grocery-app.fullname" . }}-secrets
                  key: mongo-uri-products
            - name: REDIS_URL
              value: {{ .Values.productService.env.REDIS_URL | quote }}
            - name: NODE_ENV
              value: production
          livenessProbe:
            httpGet:
              path: {{ .Values.productService.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.productService.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.productService.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.productService.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.productService.probes.liveness.failureThreshold }}
          readinessProbe:
            httpGet:
              path: {{ .Values.productService.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.productService.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.productService.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.productService.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.productService.probes.readiness.failureThreshold }}
          resources:
            {{- toYaml .Values.productService.resources | nindent 12 }}
          {{- include "grocery-app.containerSecurityContext" . | nindent 10 }}
      {{- if .Values.productService.podAntiAffinity.enabled }}
      {{- include "grocery-app.podAntiAffinity" (dict "component" "product-service") | nindent 6 }}
      {{- end }}
      {{- with .Values.productService.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.productService.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
