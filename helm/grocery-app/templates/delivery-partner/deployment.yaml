{{- if .Values.deliveryPartner.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "grocery-app.componentName" (dict "context" . "component" "delivery-partner") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "grocery-app.componentLabels" (dict "context" . "component" "delivery-partner") | nindent 4 }}
spec:
  {{- if not .Values.deliveryPartner.autoscaling.enabled }}
  replicas: {{ .Values.deliveryPartner.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "grocery-app.selectorLabels" (dict "context" . "component" "delivery-partner") | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        {{- include "grocery-app.componentLabels" (dict "context" . "component" "delivery-partner") | nindent 8 }}
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      {{- include "grocery-app.imagePullSecrets" . | nindent 6 }}
      terminationGracePeriodSeconds: 15
      {{- include "grocery-app.frontendSecurityContext" . | nindent 6 }}
      containers:
        - name: delivery-partner
          image: "{{ .Values.deliveryPartner.image.repository }}:{{ .Values.deliveryPartner.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.deliveryPartner.port }}
              protocol: TCP
          env:
            {{- range $key, $value := .Values.deliveryPartner.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: {{ .Values.deliveryPartner.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.deliveryPartner.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.deliveryPartner.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.deliveryPartner.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.deliveryPartner.probes.liveness.failureThreshold }}
          readinessProbe:
            httpGet:
              path: {{ .Values.deliveryPartner.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.deliveryPartner.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.deliveryPartner.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.deliveryPartner.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.deliveryPartner.probes.readiness.failureThreshold }}
          resources:
            {{- toYaml .Values.deliveryPartner.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
      {{- if .Values.deliveryPartner.podAntiAffinity.enabled }}
      {{- include "grocery-app.podAntiAffinity" (dict "component" "delivery-partner") | nindent 6 }}
      {{- end }}
      {{- with .Values.deliveryPartner.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.deliveryPartner.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
