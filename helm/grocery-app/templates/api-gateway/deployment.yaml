{{- if .Values.apiGateway.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "grocery-app.componentName" (dict "context" . "component" "api-gateway") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "grocery-app.componentLabels" (dict "context" . "component" "api-gateway") | nindent 4 }}
spec:
  {{- if not .Values.apiGateway.autoscaling.enabled }}
  replicas: {{ .Values.apiGateway.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "grocery-app.selectorLabels" (dict "context" . "component" "api-gateway") | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        {{- include "grocery-app.componentLabels" (dict "context" . "component" "api-gateway") | nindent 8 }}
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      {{- include "grocery-app.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "grocery-app.componentName" (dict "context" . "component" "api-gateway") }}
      terminationGracePeriodSeconds: 30
      {{- include "grocery-app.backendSecurityContext" . | nindent 6 }}
      containers:
        - name: api-gateway
          image: "{{ .Values.apiGateway.image.repository }}:{{ .Values.apiGateway.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.apiGateway.port }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.apiGateway.env.PORT | quote }}
            - name: AUTH_SERVICE_URL
              value: {{ .Values.apiGateway.env.AUTH_SERVICE_URL | quote }}
            - name: PRODUCT_SERVICE_URL
              value: {{ .Values.apiGateway.env.PRODUCT_SERVICE_URL | quote }}
            - name: ORDER_SERVICE_URL
              value: {{ .Values.apiGateway.env.ORDER_SERVICE_URL | quote }}
            - name: NODE_ENV
              value: production
          livenessProbe:
            httpGet:
              path: {{ .Values.apiGateway.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.apiGateway.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.apiGateway.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.apiGateway.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.apiGateway.probes.liveness.failureThreshold }}
          readinessProbe:
            httpGet:
              path: {{ .Values.apiGateway.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.apiGateway.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.apiGateway.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.apiGateway.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.apiGateway.probes.readiness.failureThreshold }}
          resources:
            {{- toYaml .Values.apiGateway.resources | nindent 12 }}
          {{- include "grocery-app.containerSecurityContext" . | nindent 10 }}
      {{- if .Values.apiGateway.podAntiAffinity.enabled }}
      {{- include "grocery-app.podAntiAffinity" (dict "component" "api-gateway") | nindent 6 }}
      {{- end }}
      {{- with .Values.apiGateway.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.apiGateway.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
