{{- if .Values.authService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "grocery-app.componentName" (dict "context" . "component" "auth-service") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "grocery-app.componentLabels" (dict "context" . "component" "auth-service") | nindent 4 }}
spec:
  {{- if not .Values.authService.autoscaling.enabled }}
  replicas: {{ .Values.authService.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "grocery-app.selectorLabels" (dict "context" . "component" "auth-service") | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        {{- include "grocery-app.componentLabels" (dict "context" . "component" "auth-service") | nindent 8 }}
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      {{- include "grocery-app.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "grocery-app.componentName" (dict "context" . "component" "auth-service") }}
      terminationGracePeriodSeconds: 30
      {{- include "grocery-app.backendSecurityContext" . | nindent 6 }}
      containers:
        - name: auth-service
          image: "{{ .Values.authService.image.repository }}:{{ .Values.authService.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.authService.port }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.authService.env.PORT | quote }}
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: {{ include "grocery-app.fullname" . }}-secrets
                  key: mongo-uri-auth
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "grocery-app.fullname" . }}-secrets
                  key: jwt-secret
            - name: NODE_ENV
              value: production
          livenessProbe:
            httpGet:
              path: {{ .Values.authService.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.authService.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.authService.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.authService.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.authService.probes.liveness.failureThreshold }}
          readinessProbe:
            httpGet:
              path: {{ .Values.authService.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.authService.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.authService.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.authService.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.authService.probes.readiness.failureThreshold }}
          resources:
            {{- toYaml .Values.authService.resources | nindent 12 }}
          {{- include "grocery-app.containerSecurityContext" . | nindent 10 }}
      {{- if .Values.authService.podAntiAffinity.enabled }}
      {{- include "grocery-app.podAntiAffinity" (dict "component" "auth-service") | nindent 6 }}
      {{- end }}
      {{- with .Values.authService.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.authService.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
